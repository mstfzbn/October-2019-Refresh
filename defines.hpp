#pragma once

#define DEBUG
constexpr float PI = 3.14;

static double a(1); //static vs extern

enum Dimensions
{
    ZERO,
    ONE,
    TWO,
    THREE,
    FOUR,
    FIVE
};

enum Console_Color
{
    RED = 31,
    GREEN,
    YELLOW,
    BLUE,
    MAGENTA,
    CYAN,
    WHITE
};